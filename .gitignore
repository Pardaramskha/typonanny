# Exécutable compilé (tools\build.bat le refait) et artefacts de release (tools\release.ps1)
*.exe
dist/

# Applet macOS compilé (mac/build.sh le refait)
mac/*.app/

# Réglages de l'utilisateur (les listes de ligatures livrées restent versionnées)
config/typo.conf
mac/config/typo.conf

# Aperçus générés, journaux, dépendances téléchargées (Pandoc vit dans le
# dossier partagé de la famille Stargazer, jamais ici)
exports/
logs/
bin/
mac/exports/
mac/logs/
mac/dependencies/

# Poussière
.DS_Store
Thumbs.db
